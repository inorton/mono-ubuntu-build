#!/bin/bash
set -e
if [ "x" == "x$NO_BUILD" ] 
then
	unzip $ARCHIVE_DIR/$ARCHIVE
fi

cd mono-mono-tools-dfcee14

if [ "x" == "x$NO_BUILD" ] 
then
	./autogen.sh --prefix=$MONO_PREFIX
	make
fi

PNAME=pmono-mono-tools
PVERS=3.0-dfcee14
PDEPS=pmono-mono,pmono-gtk-sharp2

set +e
