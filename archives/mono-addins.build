#!/bin/bash
set -e

tar -xf "$ARCHIVE_DIR/$ARCHIVE"
cd mono-addins-0.6.2
./configure --prefix=$MONO_PREFIX

export PNAME=pmono-mono-addins
export PVERS=0.6.2
export PDEPS=pmono-mono,pmono-gtk-sharp2,pmono-mono-tools


set +e
