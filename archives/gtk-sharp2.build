#!/bin/bash
set -e

tar -xf $ARCHIVE_DIR/$ARCHIVE
cd gtk-sharp-2.12.11
./configure --prefix=$MONO_PREFIX
make

PNAME=pmono-gtk-sharp2
PVERS=2.12.11
PDEPS=pmono-mono,libgtk2.0-dev

../../pmono_pkg $PNAME $PVERS $PDEPS "$GLOBAL_MAINTAINER" $URL 

set +e
